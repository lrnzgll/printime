<div class='container'>
  <div class='d-flex justify-content-between'>
    <h1>Lavori</h1>
    <%= link_to new_job_path do %>
      <i class="fas fa-plus fa-3x"></i>
    <% end %>
  </div>
  <ul id='jobs'>
    <%= render partial: "jobs/partials/job", collection: @content.job_card_facades, as: :content %>
  </ul>
</div>
